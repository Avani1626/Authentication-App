PORT=3000

# Optional. Path to a Firebase service account key (JSON) for production-grade
# token verification. If unset and no ./serviceAccountKey.json exists, the server
# falls back to project-id-only mode, which is enough to verify ID tokens.
# GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json
